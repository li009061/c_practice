// 假如有如下定义：int a[3][5]; 完成如下要求：
// 1.用1种方法表示 a[2][3] 的地址。
// 2.用2种完全等价的方法表示 a[2][0] 的地址。
// 3.用3种完全等价的方法表示 a[0][0] 的地址。
#include <stdio.h>

int main() {
    int a[3][5];

    int (*p1)[5] = &a[2];   
    int *p1_elem = &a[2][3]; 

    int *p2_1 = &a[2][0];
    int *p2_2 = a[2]; 

    int *p3_1 = &a[0][0];
    int *p3_2 = a[0];
    int *p3_3 = *a;

    printf("a[2][3] 地址: %p\n", &a[2][3]);
    printf("a[2][0] 地址: %p, %p\n", p2_1, p2_2);
    printf("a[0][0] 地址: %p, %p, %p\n", p3_1, p3_2, p3_3);

    return 0;
}